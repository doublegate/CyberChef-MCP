# v3.4.0 findings log

What was measured before writing anything, including what the plan got wrong and what I got wrong.
Same purpose as the v2.2–v3.3 logs.

---

## F-01 — The workflow added so a tolerance could come down had never run, and fails when it does — **FIXED**

v3.3.0 added `benchmark-baseline.yml` for one stated reason: the regression gate ships at a **50%**
tolerance, that number is a stopgap for a baseline captured on a developer machine and compared
against a GitHub runner, and the charter said plainly it *"should come down on the first
runner-captured measurement, not on the expectation of one."*

The first thing this release measured was whether that measurement exists.

```text
gh run list --workflow=benchmark-baseline.yml
(no runs)
```

**It had never run.** Manual and monthly-scheduled, added 2026-09-04, and the schedule had not yet
come round — so the whole reason it exists was still outstanding, and nothing said so, because a
workflow that has never run reports nothing at all.

Dispatched it. The `capture` job succeeded and pushed the branch. The `open-pr` job failed:

```text
##[error]GitHub Actions is not permitted to create or approve pull requests.
```

Confirmed at the source:

```json
{"can_approve_pull_request_reviews": false, "default_workflow_permissions": "read"}
```

The workflow's own design note explains why it opens a pull request: *"moving a gate's reference
point silently is indistinguishable from switching the gate off."* That reasoning is right and the
mechanism was the one thing the repository forbids — a constraint this project had already hit
twice, in `upstream-sync.yml` and `upstream-monitor.yml`, and written down.

Two defects in one workflow, and both are the same defect underneath: **it was written and never
executed.** A workflow that has not run is not a workflow, it is a plan in YAML.

---

## F-02 — The runner-captured measurement, and what it licenses — **MEASURED**

The capture that F-01 finally produced, against the committed developer-machine baseline:

```text
cross-machine drift      worst slower -23.1%   worst faster +43.6%   median -10.5%
runner's own spread      max 15.2%   median 3.6%     (4 runs, one instance)
```

The second row is the one that decides the tolerance. Once the baseline is captured **on the
runner**, the gate no longer has to absorb the cross-machine gap in the first row — only the
runner's own run-to-run variance.

That is also why 25% failed in v3.3.0 and why it is not simply reinstated here. The two false
failures then (`Entropy (100KB)` -25.3%, `Frequency distribution (100KB)` -25.5%) were
cross-machine, and a runner-captured baseline removes their cause. But 15.2% comes from **one
runner instance**, and GitHub's pool is not homogeneous; picking a tolerance from one sample is the
mistake this project has made twice already, most recently the `DIVISOR_MARGIN` study that looked
best at 1.10 over 72 cases and turned out worse than changing nothing over 400.

So: three captures, cross-instance, before the number moves.

---

## F-03 — Three Forensics operations were dead through the whole fork — **FIXED**

`Unzip`, `Untar` and `Extract Files` output `List<File>`. Through this server, none of them has
ever worked. Not degraded — dead, since v1.0.0, while all three stayed advertised in `tools/list`
and every suite stayed green.

Two independent defects stack, which is why it survived so long: fixing either one alone leaves the
operation still broken, in a different way.

### Defect 1 — `Utils.readFile` rejects the type its own signature promises

`src/core/Utils.mjs:1231`. The JSDoc example is literally
`await Utils.readFile(new File(["hello"], "test"))`, and the Node branch was:

```js
return Buffer.from(file).buffer;
```

`Buffer.from` accepts a string, `Buffer`, `ArrayBuffer` or array-like. The `src/node/File.mjs` shim
is none of those, so this throws `ERR_INVALID_ARG_TYPE`. `Chef.bake` presents every dish
unconditionally and `displayFilesAsHTML` reaches here once per member — so the operation threw
**inside the presenter, after `run()` had already succeeded**, and the caller saw an error naming a
type conversion rather than the archive.

Fixed by `patches/fork/11-utils-readfile-accepts-file.patch`. `src/core/**` is mirrored, so a
hand-edit here would be reverted by the next sync and stay reverted — which is precisely what
happened to the SafeRegex mitigation, documented at
`docs/security/2026-08-30-saferegex-reverted-by-upstream-sync.md`. The patch is verified to apply
against a pristine `git show HEAD:` copy and reproduce the working tree byte for byte.

`.buffer` is also sliced to the view's own window rather than returned whole, for the reason
`patches/fork/09` documents: a small `Buffer` is a window onto a shared 8 KB pool, and returning
the pool leaks unrelated memory to the caller. It presented here as `RangeError: offset is out of
bounds`.

### Defect 2 — the global `File` shim is never installed in this process

Five operations construct a **bare global** `File` — `Unzip`, `Untar`, `Tar`, `Zip`,
`Split Colour Channels` — and **not one of them imports it**. In a browser that is the platform's
`File`. On Node the shim at `src/node/File.mjs` stands in, and the only assignment in the tree is:

```
src/node/index.mjs:516    global.File = File;
```

That is the **generated bridge**, which this server deliberately does not import eagerly — the note
at the top of `mcp-server.mjs` explains why (it pulls all 504 operation implementations at
startup). The optimisation is right; its side effect went with it.

So `new File(...)` inside an operation resolved to **Node's own global `File`** — a `Blob` subclass
with no `.data`, whose bytes are reachable only through an async `arrayBuffer()`. The result is the
worst available failure mode: `Unzip` parsed the central directory correctly and returned members
with the **right names and zero bytes each**. That does not read as a defect. It reads as an empty
archive.

Fixed in `src/node/lib/core-recipe.mjs`, which every bake path goes through. The worker path
already gets the shim from the bridge it does import.

### Why eleven releases of a green suite never saw it

**Defect 2 cannot be reproduced in-process.** Every earlier probe — and every existing test that
touches baking — imports `bakeOnCore`, and `core-recipe.mjs` now installs the shim on import. Before
the fix, any harness that had loaded the bridge for any reason put the shim back, and the bug
vanished. Six separate hypotheses were ruled out (module duplication, the retry wrapper, caching,
`bootstrap-config`, worker dispatch, recipe normalisation — the last verified by diffing
`toCoreRecipe` output between contexts, which was byte-identical) before the difference turned out
to be the one thing an in-process test cannot vary: whether the bridge had been loaded at all.

This is the same blind spot as the three releases that shipped 524 tools with an empty
`inputSchema`, and it has the same answer. `tests/mcp/list-file-output.test.mjs` spawns the real
server and speaks to it through the official SDK client. Verified as a regression test rather than
assumed: with the shim commented out, **4 of its 6 assertions fail** — and the 2 that pass are
exactly the two that check names and the absence of markup, which defect 2 never broke.

### Scope

`Tar`, `Zip` and `Split Colour Channels` construct the same global `File` and are fixed by the same
change. The rendering of `List<File>` results is a separate decision, made in `core-recipe.mjs` and
documented there: the dish leaves the value as an array of per-file arrays on Node — deliberate
upstream behaviour that `tests/node/tests/NodeDish.mjs:226` asserts — and the presenter emits a
browser file-list table with names, sizes, two button labels, and the contents nowhere in it.
Neither gives a caller what they unzipped an archive to get.

---

## F-04 — `server.json` had never been validated by anything — **FIXED**

The file exists to be machine-read by the MCP registry. Nothing read it. Validated for the first
time in v3.4.0, it failed against the schema it declares in **six** places:

```text
/description                 153 characters, against a 100-character limit
/packages/0, /packages/1     missing `registry_type`; `registryType` not permitted
                             missing `registry_base_url`; `registryBaseUrl` not permitted
```

The cause is not a typo. The file was **written to** the 2025-09-29 schema, which is camelCase, and
**declared** the 2025-07-09 schema, which is snake_case. Both URLs resolve, both are real schemas,
and the document is internally consistent — so nothing about reading it suggests a problem. Against
the schema it was actually written to, exactly one error remains, and that one is real: the
description was 53 characters over the limit.

Fixed by declaring the schema the file is written to and shortening the description to 93
characters. `npm run check:server-json` is now the gate, wired into `core-ci.yml` and
`pull_requests.yml` beside `check:versions`.

**Why not `ajv`.** It is in `node_modules` only as a transitive dependency of something else, so a
gate built on it disappears the day that dependency drops it — silently, at a moment unrelated to
this file. More decisively, validating against the live schema URL would make the gate need the
network, and this project ships an explicit offline posture and an air-gapped deployment guide. The
rules are transcribed instead, with the retrieval date recorded, and the checker **fails when
`$schema` names a version whose rules it does not have**. That last part is the whole design: a
checker that silently checks the wrong rules is precisely the defect it was written for.

### The registry ownership proofs, both absent

Publishing to the registry needs proof of ownership per package type
(`modelcontextprotocol/registry`, `docs/modelcontextprotocol-io/package-types.mdx`, retrieved
2026-09-04):

- **npm** — `mcpName` in `package.json`, which MUST equal `name` in `server.json`.
- **OCI** — an `io.modelcontextprotocol.server.name` annotation on the image, same rule.

Neither existed. Measured rather than inferred: `npm view cyberchef-mcp mcpName` is empty across
**all eleven published versions** (2.5.0 through 3.3.0), and
`registry.modelcontextprotocol.io/v0/servers?search=cyberchef` returns `{"count": 0}`. A registry
publish today would be rejected on both packages.

Both are now present, and `check:server-json` asserts all three copies of the identifier agree —
one identifier in three files is three chances to drift, which is the same shape as the version
drift that produced `check-version-consistency.mjs`.

The OCI annotation is verified by **reading it off a built image**, not by reviewing the Dockerfile:

```text
$ docker inspect cyberchef-mcp --format '{{index .Config.Labels "io.modelcontextprotocol.server.name"}}'
io.github.doublegate/cyberchef-mcp
```

Registry publication itself is a separate, outward-facing act and is not done here.

---

## F-05 — `--requirements` does not replace `--suite all` for this server — **NO CHANGE**

The conformance CLI's own help says `--requirements <revision>` "Replaces --suite and
--spec-version", and the installed version (`0.2.0-alpha.11`, already the newest) has it. The
obvious move is to switch. Measured, it is the wrong move.

```text
--suite all                    40 scenarios   45 passed, 23 failed, all baselined
--requirements 2026-07-28      13 server scenarios, 10 of which are `tasks-*`
```

Set difference, both directions:

```text
in --suite all but not in --requirements     (none)
in --requirements but not in --suite all     13 -- 10 tasks-*, plus json-schema-2020-12
                                             and the two http-header-validation scenarios
```

This server **deliberately declines tasks**, with the reasoning recorded at `SERVER_CAPABILITIES`
and a negative test as a tripwire: tasks need state outliving the request and this server has none
by design. So switching would trade 40 scenarios for 13, ten of which exist to test a capability
that is intentionally absent and would go straight into the baseline — while losing the
`resources`, `prompts` and `caching` scenarios that are the ones actually validating v3.0.0's work.

`--suite` is still present on both the `client` and `server` subcommands in alpha.11. The gate stays
as it is. Recorded because the next person to read that help text will have the same idea, and
because if `--suite` is ever removed the replacement is not a flag swap — it is a decision about
whether ten baselined tasks scenarios are worth the three non-tasks ones they bring.

---

## F-06 — stdio does serve the 2026 era; the "downgrade" is correct — **NO DEFECT**

A hand-rolled probe made this look broken:

```text
client asks 2026-07-28 -> server answers 2025-11-25
client asks 2025-11-25 -> 2025-11-25
client asks 2025-06-18 -> 2025-06-18
```

A server that advertises both eras answering `2025-11-25` to a client asking for `2026-07-28` is
alarming, and conformance runs over **HTTP only** (`CYBERCHEF_TRANSPORT: "http"` in
`conformance/run.mjs`), so the suite could not have contradicted it.

Read at the source instead of guessed. `classifyOpeningMessage` in the SDK's `serveStdio`:

```js
if (message.method === "initialize" && !carriesValidModernEnvelopeClaim(params)) {
    return { kind: "legacy", reason: "initialize", ... };
}
```

The era is selected by an **envelope claim in `params._meta`** — `io.modelcontextprotocol/
protocolVersion` plus `io.modelcontextprotocol/clientCapabilities` — not by `protocolVersion` inside
`initialize`. A bare `initialize` is *by definition* a legacy-era handshake whatever version string
it names, and answering it with the best 2025-era version is the correct behaviour. The SDK is
explicit about why the two lists are separate: "so adding a revision here can never leak a modern
version string into a 2025-era handshake."

With a real modern opening, over stdio:

```text
server/discover  ->  supportedVersions ["2026-07-28"], ttlMs 600000, cacheScope "public"
tools/list       ->  40 tools, ttlMs 600000, cacheScope "public"
```

`cacheScope: "public"` is also correct here rather than a second bug: `serverCacheHints` returns
`private` with a 5-minute TTL when auth is on, because scope filtering makes the list vary by token,
and `public` with 10 minutes when it is off. Auth is off by default and was off in this probe.

Recorded as a negative result because the probe was wrong in a way that looked exactly like a
defect, and the next person to run the same one-liner deserves the answer.

---

## F-07 — The index default, validated against real client limits — **MEASURED**

Two client constraints were worth checking against the actual surface rather than assuming:

```text
surface     tools   payload    longest description   over 2048B   over a 128-tool cap
index         40     40,962           614                 0              no
curated      118    104,208           614                 0              no
all          543    421,366           614                 0             YES
```

- **A 128-tool client cap** is cleared by `index` and `curated` and exceeded only by `all`, which
  is not the default. The default was chosen on payload size; this is the first number showing it
  also clears a hard structural limit, with 88 tools of headroom.
- **A 2 KB tool-description limit** truncates nothing here. The longest description is 614 bytes
  and the mean at `index` is 245.

The surface figures themselves had drifted: AGENTS.md carried 40,637 / 103,883 / 421,041, and all
three are 325 bytes low. Nothing on this branch touches the tool surface (`git diff --name-only
master..HEAD` contains no `tool-*`, `mcp-server.mjs` or `OperationConfig` path), so the drift
predates v3.4.0 — the row was recorded during v3.3.0's development and never re-measured before the
tag. That is the failure mode the row's own text warns about, which is the argument for keeping
`npm run measure:surfaces` rather than the numbers.

---

## F-08 — The tolerance comes down from 50% to 20%, on cross-instance evidence — **DONE**

F-02 said three cross-instance captures before the number moves. There are three, and the artifacts
survived the `open-pr` failure, so the study could be done without re-running anything.

```text
between the three captured medians (30 tasks)   worst  6.7%   median 1.5%
within one capture (4 runs), pooled  (n = 90)   worst 15.2%   median 3.6%   1 of 90 above 15%
```

Simulated with each capture as the baseline and the other two as the run under test — 180
comparisons — there are **zero** false failures at 20%, 15% or even 10%. Verified afterwards
through the real gate rather than a reimplementation of it: both other captures run against the
committed baseline via `node benchmarks/check-regression.mjs` report "No regression", exit 0.

20% rather than the 10% the data would also support, because three instances over sixteen minutes
is a narrow sample of a pool that is not homogeneous, and picking a threshold from the sample it
was measured on is the mistake behind the `DIVISOR_MARGIN` study. 20% is 3x the worst observed
cross-instance spread and clears the worst single-capture spread.

**The baseline itself moved to the runner**, which is the change that makes the tolerance mean
anything: the committed developer-machine baseline sat at a median -9.6% offset from the runner,
worst +43.1%, so a regression smaller than that offset was never catchable on CI.

### A capture that lied about where it ran

`capture-baseline.mjs` spread the previous baseline over the new one (`...existing`) and only
overwrote some fields, so the first runner capture inherited:

```text
_machine:             "Captured on one developer machine..."
toleranceRationale:   "...on an otherwise idle machine. CI variance is NOT yet measured"
```

Both false the moment the workflow ran, and a provenance field that lies is worse than no
provenance field — the whole purpose of the record is to say what the numbers may be compared to.
It now derives provenance from `GITHUB_RUN_ID`/`RUNNER_OS`/`RUNNER_ARCH` and says plainly, in the
local case, that the baseline is not from the machine the gate runs on.

Full study: [`docs/internal/measurements/v3.4.0-runner-baseline.md`](measurements/v3.4.0-runner-baseline.md).

---

## F-09 — The shell-free base image: it works, and it is still the wrong trade — **MEASURED, NO CHANGE**

Carried forward twice without a measurement. Measured here by building it, not by reading tag
descriptions.

`cgr.dev/chainguard/node:latest-slim` genuinely has no shell (`--entrypoint /bin/sh` fails) against
the current base's 231 binaries in `/usr/bin` including `npm`. The build needs one other change:
the `HEALTHCHECK` is in shell form (`CMD node -e "..." || exit 1`) and must become exec form, which
was done and works.

```text
                      current base        latest-slim
image size            475 MB              449 MB          -26 MB, -5.5%
node                  v26.8.1             v25.9.0
shell                 BusyBox present     none
```

Every fragile operation returns **byte-identical** output on both — the wasm path (`Argon2`
produced the same `$argon2i$v=19$m=1024,t=2,p=1$...` encoded hash), the vendored crypto-api path
(`HMAC`, `SM3`), the OpenSSL-legacy-provider path patch 06 exists for (`LM Hash`), the
capstone-js 5 path patch 05 exists for (`Disassemble ARM`), and `Generate all hashes`. Ten of ten,
through a real MCP client against a running container.

**So it works, and it is still declined.** The 26 MB costs a Node version: `latest-slim` ships
**v25.9.0** against the current **v26.8.1**. That is a *downgrade* for every existing user, and it
is a version **neither CI job tests** — the matrix is 24 (the declared floor) and 26 (what ships),
added in v2.8.1 precisely because the runtime users get was previously never exercised. Shipping an
untested runtime to save 5.5% is the same class of trade that mistake was.

There is no `node26-slim` or `26-slim` tag; `latest-slim` is the only slim variant Chainguard
publishes for this image.

**What would change the answer**, stated so it can be checked rather than re-argued: a Chainguard
`node` slim tag on Node 26. At that point the trade is 26 MB against losing `docker exec`
debugging, which is a real but much smaller objection — and the HEALTHCHECK change and the
operation evidence above are already done.

---

## F-10 — An out-of-process test contributes no coverage — **FIXED**

`tests/mcp/list-file-output.test.mjs` has to spawn a real server, for the reason F-03 gives: the
defect it catches is invisible to any harness that imported the bridge. But the instrumentation runs
in *this* process and the server runs in another, so the rendering block measured as **completely
untested** and the `src/node/lib/**` gate failed at 98.68% lines against a 99% threshold.

The temptation is to lower the threshold, and it is exactly wrong: the code is tested, the coverage
tool simply cannot see it, and a threshold lowered to accommodate a measurement artefact stays
lowered for real gaps afterwards.

The rendering is not the part that needs a subprocess. Only the missing global `File` was invisible
in-process, and importing `bakeOnCore` installs it. So a second `describe` block exercises the
branches directly, where they can be counted, and the two suites assert different things: the
in-process one that the rendering is correct, the out-of-process one that it is reachable at all.
`core-recipe.mjs` goes from 84.61% statements to 100%.

Worth stating as a general rule, because this project will write more real-client tests: **a
real-client test proves reachability and earns no coverage; pair it with an in-process test of the
logic.** Neither substitutes for the other.

## F-11 — Release housekeeping found in passing

- The **benchmark tolerance was documented as 25% in AGENTS.md** while `baseline.json` carried 50%.
  It was 25% for about an hour in v3.2.0 before CI disagreed, and the row was written in that hour.
- The **surface figures were 325 bytes low across all three surfaces**, recorded during v3.3.0's
  development and never re-measured before the tag — which is the thing that row's own text warns
  about, happening to that row.
- The `[Unreleased]` CHANGELOG section held an item that had already shipped to `master`.

None of these is interesting alone. Together they are the argument for `check:versions` and
`measure:surfaces` existing at all: every one is a number a human wrote down once and no gate reads.

---

## F-12 — What the PR review found, and the one that changed a design — **FIXED**

Twelve findings across three reviewers on PR #118, every one verified against the code before being
answered. Eleven were valid. The interesting one is not the count.

### The finding that was wrong, and how that was established

Antigravity blocked on *"never hand-edit files under `src/core/`... revert your direct edits and let
the sync script apply the patch."* Following it would have removed the fix from the shipped code: a
`patches/fork/*.patch` is the mechanism for a change **surviving a future sync**, not an alternative
to the change being in the tree. Established against `master` rather than argued:

```text
$ git show master:src/core/operations/AddTextToImage.mjs | grep -n "FORK CHANGE"
141:  // FORK CHANGE (patches/fork/10): ...
273:  // FORK CHANGE (patches/fork/09): ...
$ git show master:src/core/operations/LMHash.mjs | grep -c "node-forge"
3
```

Three existing patches, all present in `src/core/` with the same marker. No change made.

### The finding that changed a design

CodeRabbit: `recoverFromPair` only tries `s1 - s2`, but a shared `r` also occurs when the nonce is
`n - k` — `-kG` has the same x-coordinate — which is exactly what **low-S normalisation** (BIP-62)
produces. Confirmed numerically: for such a pair `s1 - s2` yields a wrong key and `s1 + s2` the
right one.

The obvious fix is to try both and keep whichever verifies. **That verification is vacuous**, which
was written, measured, and only then understood. Given any denominator `D`:

```text
k = (z1 - z2)/D  and  d = (s1 k - z1)/r   =>   s1 = k^-1 (z1 + r d)   by construction
k D = z1 - z2  and  k s1 = z1 + r d       =>   k^-1 (z2 + r d) = s2   follows from it
```

So *both* candidates reproduce *both* signatures exactly, and a check against the pair
discriminates nothing while looking like it does. Verified: both passed.

The pair genuinely does not determine the key. Choosing needs the public key, and testing a
candidate against a public point needs the group law — an elliptic-curve implementation in a tool
that otherwise needs one integer per curve. So the tool returns **both candidates**, each labelled
with the assumption it rests on, plus a `how_to_choose` field saying why the pair cannot decide and
what will. That is a better answer than picking one and being silently wrong half the time, and it
is the same exactness-honesty rule the biased-nonce refusal follows.

### The rest, all valid

| Finding | Why it was real |
|---|---|
| `hashBits` from the raw string while `parseInteger` strips separators | `ab:cd:...` measured 97 chars → 384 bits on a 256-bit curve. Second instance of this exact mismatch; the rule is now shared via `cleanIntegerText` |
| Only adjacent pairs checked in an `r` group | A duplicate at (0,1) hid a recoverable pair at (0,2), against a schema promising "every pair" |
| ASCII-only printable test | `résumé.txt` came back as base64. Now a C0-control scan plus a strict UTF-8 decode — `fatal: true`, because the default returns U+FFFD and a string that looks fine |
| `--force-with-lease` without fetching the branch | `actions/checkout` fetches only the default branch, so every run after the first would fail with `stale info` |
| The workflow still said to bring 50% down | It came down in this release |
| A local capture inherited a runner's `capturedOnRunnerRunId` | `...existing` carried it while `_machine` said developer machine — the same defect the `_machine` fix was for |
| Chart `version` not bumped | It has moved every release: 0.4.0 → 0.5.0 → … → 0.5.3. Now 0.5.4 |
| `values.yaml` digest example still `:3.3.0` | Same class as the compose prose missed three times |
| `tool-catalog.mjs` header still pre-v3.3.0 | A file the doc pass missed entirely |
| `check-server-json` checked presence, not types | `"transport": "stdio"` passed. Now types, transport shape, and 11 regression fixtures |
| The reproduction formula mixed units | Percentage points against a ratio |

`bytes.every(...)` was separately flagged as blocking on cost. Measured: 112 ms on 16 MB against a
9 ms encode. The suggested prefix scan is not taken — it would decode a member that is text for
4 KB and binary after as UTF-8 — but a plain loop is 5.9x faster with the guarantee intact.

---

## F-13 — The 20% tolerance was wrong, and CI proved it within the hour — **REVERTED**

F-08 concluded 20%, on three runner captures with a worst cross-instance spread of 6.7% and zero
false failures across a 180-comparison simulation. Four real CI runs on `release/v3.4.0` split
two-pass / two-fail:

```text
To Hex (100KB)                  -41.8%
To Hex (10KB)                   -37.0%
Frequency distribution (100KB)  -29.2%
Entropy (100KB)                 -26.2%
Regular expression (1KB)        +28.2%   <- on the SAME run
Regular expression (10KB)       +24.0%
```

**Both directions at once**, on tasks no commit on the branch touches. Ruled out rather than
assumed: `benchmarks/operation-benchmarks.mjs` imports `bake` from `src/node/index.mjs`, and the
generated bridge does not import `core-recipe.mjs` or reach any other changed module — so nothing in
this release is in the benchmarked path. The two passing runs cover identical benchmarked code.

It is a different host class. The memory-bandwidth-bound tasks (`To Hex`, `Entropy`,
`Frequency distribution`, all at 100KB) collapsed while the CPU-bound `Regular expression`
improved — a profile difference, not a regression.

### What was wrong with F-08, precisely

Not the arithmetic. The **sampling**: three captures at 11:13, 11:29 and 11:35 UTC almost certainly
landed on one class of host, and three similar machines cannot characterise a heterogeneous pool.

The uncomfortable part is that F-08 knew this. It chose 20% over the 10% its data allowed with the
words *"three instances over a sixteen-minute window is a narrow sample of a pool that is not
homogeneous"*, cited the `DIVISOR_MARGIN` study as the precedent for that exact error — and then set
a threshold from that sample anyway. Naming a bias is not controlling for it. The safety margin was
applied to the wrong quantity: 3x a spread measured on one host class is still a number about one
host class.

### What stands

The **baseline being runner-captured** is the real result of this work and is unaffected. It removes
a systematic -9.6% median offset (worst +43.1%) that the tolerance previously had to absorb *on top
of* pool variance, so 50% today bounds strictly less than 50% did before.

### What would actually fix it

The tolerance is the wrong lever and a wider one is no better. Two mechanisms target the real cause;
neither is a release-day change:

1. **Median of several runs in CI.** The baseline is a median of four and the gate compares a
   *single* run against it — asymmetric. Cuts within-instance noise, not host-to-host difference,
   and triples the job's runtime.
2. **Normalise against a calibration task.** Measure one fixed, well-understood operation each run
   and scale the rest by it. This targets host-class difference directly, which is the thing that
   actually broke this, and it is the one worth building.

Recorded at length because the failure is more instructive than the fix: a measurement can be
arithmetically sound, honestly reported, aware of its own weakness, and still wrong — if the sample
does not span the population the threshold has to survive.

