# v3.9.0 findings log

Written during development, in the order the findings arrived. The convention since v2.5.0 is that
F-01 states what the plan got wrong, before any code is written.

---

## F-01 — There is no v3.9.0 charter, and that is the finding

Every release since v2.5.0 opened by measuring a plan against the running server. This one has no
plan to measure. `docs/planning/v3/charters/` holds five files:

```text
v3.1.0-evaluation-harness.md    shipped v3.1.0, re-scoped on measurement
v3.2.0-result-efficiency.md     shipped v3.2.0
v3.3.0-external-tools.md        live -- the ext-proj-int programme
v3.x-supply-chain.md            absorbed into v3.2.0 Track B
v4.0.0.md                       live -- deliberately unscheduled, spec-driven
```

Nothing names v3.9.0. `RE-MEASURE.md` step 6 says: *"If the charter is empty, say so and pick
different work."* There is no charter at all, which is the same instruction with less to read.

So the scope is **derived from measurement rather than from a plan**, out of four sources: the
carried-forward list as v3.8.0 F-02 left it, the open issue tracker, the open Dependabot pull
requests, and a full dependency and toolchain sweep. Each finding below records what was measured.

**The version number.** MINOR under SemVer. Nothing here changes an interface a caller depends on:
the bumps are compatible by construction (F-04), the two behaviour changes are a workflow's
comparison ref (F-02) and an ordering the server never promised (F-03), and no tool schema, error
code, or protocol surface moves.

### The carried-forward list, as v3.8.0 left it

| Item | State entering v3.9.0 |
|---|---|
| arm64 performance | Built in v3.8.0. Off the list. |
| `cert_chain` | Built in v3.8.0. Off the list. |
| Task-level scoring | Declined in writing (`docs/planning/v3/task-level-scoring.md`). Off the list, not deferred. |
| Shell-free base image | Live. Trigger re-measured this release -- F-06. |
| Opt-in feature adoption | Permanently deferred; needs telemetry this project deliberately does not collect. Unchanged. |

Two live items, and one of them is a re-measurement rather than work.

---

## F-02 — Issue #129 says a release is "available" that this repository already ships

[#129](https://github.com/doublegate/CyberChef-MCP/issues/129), opened by `upstream-monitor.yml` on
2026-09-06, is titled **"New CyberChef release v11.4.0 available"** and reports one new operation
(`ParsePGPKey.mjs`) and thirty modified ones.

Measured:

```text
package.json cyberchefUpstreamVersion   11.4.0
gchq/CyberChef latest release           v11.4.0  (published 2026-08-18)
```

This repository is **already on v11.4.0**. Diffing the operation set against the v11.4.0 tag:

```text
upstream v11.4.0: 505 .mjs      local: 506 .mjs
in local, not upstream:  index.mjs      (generated by grunt, gitignored)
in upstream, not local:  (none)
```

Every one of upstream's 505 operation files is present. The set is identical.

And `ParsePGPKey.mjs`, the "new operation", **is not in the v11.4.0 tag at all** --
`GET /repos/gchq/CyberChef/contents/src/core/operations/ParsePGPKey.mjs?ref=v11.4.0` returns 404.

### Why the workflow says otherwise

`upstream-monitor.yml` reads two different refs and prints them as one:

| Line | What it does | Ref |
|---|---|---|
| 40 | `git pull origin master` | **master** |
| 61 | `LATEST_TAG=$(git describe --tags --abbrev=0)` | **the newest tag** |
| 98, 108 | diffs operations against the clone's working tree | **master** |

So the headline is a tag name and the numbers underneath describe unreleased upstream master. The
issue is not wrong about there being a difference -- there is one, `ParsePGPKey` and thirty
modified files are real work on upstream `master`. It is wrong about what that difference *is*: it
presents post-release drift as an unadopted release.

This is the failure mode this repository keeps finding in its own checks, and the same one v3.8.0's
F-04 found in the benchmark workflow: not a check that fails to run, but a check whose output means
something other than what it says. A maintainer reading #129 would start an upstream sync that has
nothing to sync.

**Fixed this release.** The comparison is pinned to the same ref the headline names, and the issue
body distinguishes "a new release exists" from "master has moved since the release you are on".

---

## F-03 — `magic` ranks a decoded plaintext below the container it came out of

Filed as [#130](https://github.com/doublegate/CyberChef-MCP/issues/130) before this release opened,
from a live exercise of the 3.8.0 image.

A three-layer payload (`Gzip` -> `To Base64` -> `To Hex`) returns four candidates, with the fully
decoded plaintext at **rank 3**, beneath two partial decodes -- one of which is raw gzip bytes.

Recomputing every term of the comparator at `src/core/lib/Magic.mjs:328`:

| Recipe | lang score | after clamps | +len | +entropy | final |
|---|---|---|---|---|---|
| `From Hex -> From Base64` | 421,308.72 | **500** (gzip clamp) | 2 | 6.62 | **508.62** |
| `From Hex` | 4,853.05 | 4,753.05 | 1 | 5.79 | 4,759.84 |
| `From Hex -> From Base64 -> Gunzip` | 5,575.92 | 5,475.92 | 3 | 4.52 | **5,483.44** |
| (none) | 6,924.19 | 6,824.19 | 0 | 3.58 | 6,827.77 |

Two effects, and the first reading of this bug got the cause wrong:

1. **The file-type clamp decides rank 1.** `if (a.fileType && aScore > 500) aScore = 500` collapses
   421,308 to 500. Any candidate matching a file signature lands near 508 while a text candidate
   keeps a chi-squared score in the thousands, so a detected *intermediate container* outranks the
   decoded content of that container -- systematically, and worse the deeper the nesting.
2. **The chi-squared scorer rated base64 above English**, 4,853 against 5,576, deciding rank 2
   versus rank 3. That is the estimator weakness already documented at `src/node/lib/magic.mjs:126`
   surfacing in the ordering rather than in a label.

**Recipe length is not the cause.** It contributes 3 against 1 -- two points of a 723-point gap --
and entropy actually favours the correct answer. The `aScore += a.recipe.length` line is a
plausible-looking line that fits the symptom, and reading the mechanism off it without doing the
arithmetic is how the wrong cause got asserted. The table above is what settled it.

`src/core/lib/Magic.mjs` is upstream verbatim; no `patches/fork/*.patch` touches it. Fixed in the
MCP layer, where this project owns the code.

---

## F-04 — The dependency sweep: what moves, what is held, and what the holds cost

`npm outdated` reports twenty packages. They are not one category.

### Taken

| Package | From | To | Note |
|---|---|---|---|
| `@noble/hashes` | 2.3.0 | 2.4.0 | Dependabot #124. **Upstream also moved to 2.4.0** -- this is alignment, not drift. |
| `otpauth` | 9.5.1 | 9.5.2 | Upstream moved to `^9.5.2`. Alignment. |
| `@codemirror/state`, `@codemirror/view`, `autoprefixer`, `cspell`, `dompurify`, `eslint`, `eslint-plugin-jsdoc`, `postcss`, `tinybench`, `webpack-bundle-analyzer`, `zod` | various | latest | All already satisfied by the declared range; a lockfile refresh, not a range change. |

### Held, with the hold re-verified rather than assumed

`.github/dependabot.yml` already ignores three majors, each with measured evidence written down.
The stated re-evaluation trigger is *"re-evaluate when upstream moves"*. Upstream has not moved:

| Package | Our range | Upstream master pins | Recorded reason |
|---|---|---|---|
| `@xmldom/xmldom` | `^0.8.15` | **`^0.8.15`** | 0.9 was *tried*: `CSS selector` returns 0 matches, `XPath expression` 1 of 2, through the real pipeline. Behavioural, not a rename. |
| `geodesy` | `1.1.3` | **`1.1.3`** | v2's `LatLon` subclasses do not compose -- `utm.js` gives `toUtm` without `toOsGrid`, `osgridref.js` the reverse, and `ConvertCoordinates.mjs` needs both on one object. |
| `jq-web` | `^0.5.1` | **`^0.5.1`** | 0.6's module namespace exports `then`, making it a thenable; `await import("jq-web")` throws before any operation runs. Broke 10 test files. |

All three trigger conditions are unfired. The holds stand, and stand on the same evidence rather
than on inertia. Taking any of them would mean carrying a fork patch against upstream-owned code
that upstream will keep changing -- the exact shape of the SafeRegex loss documented in
`docs/security/2026-08-30-saferegex-reverted-by-upstream-sync.md`.

`grunt` and `grunt-*` remain ignored for the reason the config gives: the build orchestration is
adopted wholesale from upstream by the sync.

### `@astronautlabs/amf`

`npm outdated` reports current 0.0.6, latest **0.0.1**. The `latest` dist-tag points at an older
version than the one published. Not a bump; noted so the next sweep does not re-investigate it.

---

## F-05 — Three major versions of `actions/checkout` in one repository

The Dependabot pull requests name four actions. The sweep found the drift is wider than the PRs:

```text
actions/checkout     v7 x16    v6 x3    v5 x2    + one SHA pin
actions/setup-node   v7 x10    v6 x1    v5 x2
```

Dependabot raised #125 (`checkout` 5 -> 7) and #126 (`setup-node` 5 -> 7) because it reports the
*lowest* version it finds. The v6 occurrences are invisible to those PRs and would have survived
merging them -- `helm-chart.yml` and `benchmark-baseline.yml` would still be on v6 with both PRs
green. Fixing the ecosystem one Dependabot PR at a time converges slowly and never reports done.

Everything else is already current: `upload-artifact` v7, `download-artifact` v8, `github-script`
v9, `configure-pages` v6, `deploy-pages` v5, `upload-pages-artifact` v5, `trivy-action` v0.36.0,
`codeql-action` v4, `setup-buildx-action` v4, `build-push-action` v7, `metadata-action` v6,
`login-action` v4, `codecov-action` v7. Two need majors: `docker/setup-qemu-action` v3 -> v4 (#127)
and `azure/setup-helm` v4 -> v5 (#128).

### The SHA pin is correct and stays

`antigravity-review.yml` pins `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7`
because that job runs on a self-hosted runner. Verified rather than assumed:

```text
pinned SHA          3d3c42e5aac5ba805825da76410c181273ba90b1
tags/v7    resolves 3d3c42e5aac5ba805825da76410c181273ba90b1
tags/v7.0.1 resolves 3d3c42e5aac5ba805825da76410c181273ba90b1
```

The pin is exactly current. A blanket `@v7` rewrite across the workflows would have *weakened* this
one, which is why the sweep is per-file rather than a `sed`.

---

## F-06 — The shell-free base image trigger has still not fired, and the runtime moved a patch

Two separate measurements against `cgr.dev/chainguard/node`, taken by running the images:

```text
cgr.dev/chainguard/node:latest-slim    node v25.9.0
cgr.dev/chainguard/node:latest         node v26.8.2
```

**The slim tag is unchanged at v25.9.0**, exactly where v3.8.0 found it, and still an entire major
behind what this image ships. Only `latest`, `latest-dev` and `latest-slim` are pullable
anonymously -- the registry lists 1000 tags and every versioned one is behind the paid tier -- so
`node26-slim` cannot be adopted even if it exists. Declined a third consecutive release, on the
same measurement, and the trigger stays as written.

**The runtime moved**: `latest` is now Node **v26.8.2**, against the **v26.8.1** the pinned digest
resolves to. That is a real patch bump to the shipped image, and re-resolving the two digests is
this release's Docker work.

Node itself: v26.8.2 (2026-09-09) is the newest line, v24.21.0 is LTS Krypton. **There is no Node
27**, so `engines: ">=24 <27"` still spans exactly the supported range and does not widen.

---

## F-07 — The MCP ecosystem did not move at all

Re-checked as the ritual's step 3 requires, because the last three releases each found something
here:

```text
@modelcontextprotocol/server        2.0.0            unchanged
@modelcontextprotocol/node          2.0.0            unchanged
@modelcontextprotocol/client        2.0.0            unchanged
@modelcontextprotocol/sdk           1.30.0           unchanged (devDependency, legacy-client proof)
@modelcontextprotocol/conformance   0.2.0-alpha.11   unchanged; `latest` tag is still 0.1.16
server.json schema                  2025-12-11       newest; registry ships no later dated schema
MCP specification                   2026-07-28       still Current; no revision after it
```

The schema check was done two ways rather than one, because a single 404 proves less than it looks:
the registry's own `internal/validators/schemas/` directory tops out at `2025-12-11.json`, and no
later dated URL resolves under `static.modelcontextprotocol.io/schemas/`.

Nothing to do. v4.0.0 stays unscheduled for the same reason it did last release -- it is defined by
a specification revision that has not happened.

---

## F-08 — vitest 5 landed inside this project's own cooldown

`vitest` and `@vitest/coverage-v8` went to **5.0.0 on 2026-09-03**, seven days ago. There is no
Dependabot PR for it because `.github/dependabot.yml` sets `semver-major-days: 14`, with a written
rationale: *"nothing here is urgent enough to justify being the first to find out."*

Taking it at day 7 overrides the project's own stated policy, so it needs a better reason than
"latest". The reason is that the cooldown exists to avoid being the first to find out, and running
the full suite **is** finding out -- a devDependency test runner is exactly the case where the
evidence is cheap and immediate, unlike the cryptography and parser libraries the policy was
written for.

Read against this repository's configuration, the vitest 5 breaking changes are mostly inert:

| Change | Effect here |
|---|---|
| `coverage.thresholds.perFile` no longer inherits to globs | **None.** `perFile` is set nowhere, deliberately, and `vitest.config.mjs` carries three paragraphs explaining why. |
| `coverage.include`/`exclude` match relative paths, no "contains" matching | The real risk. Every entry is already relative or a `**` glob, but coverage numbers decide a gate, so this is settled by running it. |
| `clearMocks` defaults to `true` | Risk. Settled by running it. |
| Unawaited async assertions now fail | Would surface a latent defect rather than create one. |
| Vite >= 6.4.0 required | Satisfied: 8.2.2 installed. |
| Node >= 22.12.0; engines `^22.12.0 \|\| ^24.0.0 \|\| >=26.0.0` | Satisfied at both CI ends, 24 and 26. Note it excludes Node 25, which this project does not test. |
| `test.sequential` removed | Not used anywhere in `tests/`. |

Decision: **attempt it and let the suite decide.** The outcome is recorded in F-09 rather than
predicted here.

---

## F-09 — vitest 5 taken, on measurement rather than on "latest"

F-08 left the decision to the suite. The suite decided:

```text
npm run test:mcp        1697/1697 across 73 files      (before any v3.9.0 test was added)
npm run test:coverage   exit 0
                        96.54 lines / 89.53 branches / 96.54 functions / 95.49 statements
```

Against the v3.8.0 figures of 96.6 / 89.5 / 96.5 / 95.5, the measured surface is unchanged to
within a rounding step. **The `coverage.include`/`exclude` relative-path change -- the one breaking
change that could have moved a gate -- moved nothing**, which is the specific thing F-08 said would
have to be settled by running it rather than by reading the migration notes.

`clearMocks` defaulting to `true` broke nothing, and no unawaited assertion surfaced. Core suites
were re-run because `@noble/hashes` and `otpauth` land in operations, not in the MCP layer:
241/241 Node API and 2289/2289 operations.

Taken. The cooldown in `.github/dependabot.yml` stays at 14 days for majors, unchanged: this is one
devDependency where the evidence was cheap and immediate, not a reason to lower the bar for the
cryptography and parser libraries the policy was written for.

---

## F-10 — The upstream monitor could never report "up to date"

Found while fixing F-02, and larger than F-02.

The `Get current version` step read the wrong field:

```bash
CYBERCHEF_VERSION=$(node -p "require('./package.json').version")     # 3.8.0 -- this fork's semver
```

`package.json` carries **two** versions, and has since v2.2.0: `version` (this fork, 3.8.0) and
`cyberchefUpstreamVersion` (the upstream base, 11.4.0). The comparison step then does:

```bash
CURRENT="v${CYBERCHEF_VERSION}"        # "v3.8.0"
if [ "$LATEST" != "$CURRENT" ]; then   # "v11.4.0" != "v3.8.0"  -- ALWAYS true
```

So `new_version=true` on **every run, forever**. The monitor is not flaky and it is not
occasionally wrong; it is structurally incapable of reporting the "Already up-to-date" branch its
own code contains. The dead branch is right there in the file and has never once executed.

The same field also generated the issue's "next steps" link:

```text
https://github.com/gchq/CyberChef/compare/v3.8.0...v11.4.0
```

`v3.8.0` is not a ref in `gchq/CyberChef`. The link has been broken in every issue this workflow has
ever opened.

Both fixed. Together with F-02 this is one workflow making three different claims from two wrong
sources -- and the reason it survived is that nobody re-reads a bot's output closely when the
headline looks plausible.

---

## F-11 — The first version of the #130 fix regressed a case that was already correct

The re-ranking rule (F-03) was first written as: promote any candidate whose recipe extends
another's and which produced UTF-8 text. That is the broad reading of the issue's own suggestion,
and it is wrong.

Measured on the single-byte-XOR payload, which ranked **correctly** before the change:

```text
before   rank 1  From Base64 -> XOR                    "The key is a single byte and the analyst..."
after    rank 1  From Base64 -> XOR -> From Hexdump    "X"
         rank 2  From Base64 -> XOR                    "The key is a single byte and the analyst..."
```

`From Hexdump` extends the correct recipe and returns a single character at entropy 0. It is
incidental UTF-8, not a decoding, and the broad rule promoted it over the answer. A fix for a
ranking bug had introduced a ranking bug, in the opposite direction.

The rule is now anchored on the **container**: promotion happens only past a candidate that
produced a detected file type. That is what makes it mean "you decoded the container" rather than
"you ran one more operation".

Requiring a positive language probability instead was considered and **checked rather than
assumed** -- it would also have been wrong, because the correct answer in the issue's own payload
scores no language at all (every `languageScores` probability is 0), so a language test would
suppress the exact promotion the fix exists to make.

Three regression tests pin all of it, including the order-preserving claim. Verified by disabling
the re-rank: the headline test fails, the two guard tests pass either way, which is what a guard
test should do.

---

## F-12 — v3.8.0 published surface figures that its own artefact contradicts

The surface figures moved this release -- 44,406 -> 44,493 for the index, and +87 uniformly across
all three surfaces. The obvious assumption is that v3.9.0 changed something. It did not.

A/B against the **published v3.8.0 image**, through a real MCP client, same method as
`npm run measure:surfaces`:

```text
ghcr.io/doublegate/cyberchef-mcp_v3:3.8.0   index: 42 tools, 44493 bytes
this working tree (v3.9.0)                   index: 42 tools, 44493 bytes
per-tool byte maps                           IDENTICAL
```

The shipped v3.8.0 artefact serves 44,493. Its release notes say *"Surfaces re-measured: 42 tools /
44,406 bytes"*. **The number was already wrong at the tag** -- measured during development, changed
before the tag, never re-measured. Which is, word for word, the failure the `AGENTS.md` row itself
warns about:

> Every figure here was 325 bytes low through v3.3.0 -- recorded during its development and never
> re-measured before the tag, **which is this row's own warning happening to this row**.

It happened to that row again, one release later.

### Why the gate did not catch it

`tests/mcp/tool-surface-figures.test.mjs` compares every document against the canonical table in
`tool-catalog.mjs`'s header. That establishes **agreement, not truth**. When the table itself is
stale, every document agrees with a stale number and all four tests pass -- which is precisely what
`registry-tool-docs.test.mjs` warns about in its own header, about this very file:

> `tool-surface-figures` can only establish that documents agree with each other -- three documents
> can be consistently stale and it passes.

The warning was written in v3.8.0. The thing it warned about was already true in v3.8.0.

**Fixed by measuring.** A fifth test spawns a real client against each of the three surfaces and
compares the live byte count to the recorded table, exactly. No tolerance: these are deterministic
serialisations of a fixed catalogue, and a tolerance would let this same drift accumulate under it.
Verified by reverting the table to 44,406 -- it fails with `table says 44406, server serves 44493
(+87)` and names the command that fixes it.

The published v3.8.0 figures are left as they are, in the CHANGELOG and that release's notes. They
record what was claimed at the time, and rewriting history to hide a wrong number is worse than the
wrong number.

---

## F-13 — vitest 5 detonated a landmine that has been in `mcp-server.mjs` all along

The first CI run failed `mcp-test` on **both** Node versions and `test (node 24)`, after passing
locally twice. PR #123, the last merged PR, was green on those same jobs under vitest 4 — so this
is new to this branch, and vitest 5 is the only plausible cause.

The crash, from the job log:

```text
{"level":"error", "error":"ENOENT ... '/tmp/cyberchef-ratelimit-J9g5xR/recipes.json...tmp'"}
{"level":"fatal", "error":{"message":"Failed to save recipe storage: ENOENT ..."}}
```

Two facts, both verified rather than assumed:

1. **`runServer()` is called at module scope in `mcp-server.mjs`, with no main-module guard.**
   Confirmed by importing the module in a bare script: the full startup banner appears. So every
   test file that imports it starts a real stdio server in that test process, holding a live recipe
   manager — and `runServer().catch` ends in `process.exit(1)`.
2. **Four test files repoint `CYBERCHEF_RECIPE_STORAGE` at their own temp directory, import the
   server, and then `rm -rf` that directory in `afterAll`.** When a save lands after the directory
   is gone, the save rejects, the catch fires, and `process.exit(1)` kills the vitest worker
   mid-file. `rate-limit-dispatch`, `in-process-handlers`, `prompts-resources` and `tenancy` all
   qualify; only the first fired, because the race is timing-dependent and CI is slower.

Not a vitest 5 defect. Vitest 5 changed the timing enough for a pre-existing race to land, and the
race is only fatal because a library module arms `process.exit` on import.

**Fixed by removing the race, not the symptom**: those four directories are no longer deleted
mid-run, matching what `tests/mcp/setup/recipe-store-isolation.mjs` already does deliberately and
for the same underlying reason ("vitest reuses worker processes across files"). `tenancy`'s second
temp directory is still removed, correctly — it belongs to its own `RecipeStorage` instance and no
module-level server ever sees it.

**The structural defect is recorded and NOT fixed here.** A library module should not start a
server and should not call `process.exit` when imported. Guarding `runServer()` behind a
main-module check is the right fix and changes the entry-point semantics of the most important file
in the repository — that deserves its own release and its own review, not a hurried commit during a
bot-review ceremony. Carried forward.

---

## F-14 — The review round: five real findings, two rejected, one that was my own new bug

Eight threads across Copilot and CodeRabbit. Each was checked against the code before it was
answered.

| # | Finding | Verdict |
|---|---|---|
| Copilot | The tag checkout leaves `ref-proj` detached, and `Commit ref-proj updates` then commits a tag checkout under a message saying "latest upstream" | **Right, and it was mine** — introduced three steps earlier by the F-02 fix. Restored to master before the commit step. |
| Copilot | `ROADMAP.md` `Last Updated` still 2026-09-04 | **Right.** Fixed. |
| CodeRabbit | `extendsRecipe` compares op names only, so sibling branches with different args count as one path | **Right, and measured**: on one intensive payload, 6 of 11 distinct op-paths carried more than one argument set, `From Base64 -> XOR` alone carrying **seventeen**. Now compares args structurally, with a regression test that fails against the old behaviour. |
| CodeRabbit | The ranking test asserts on `rank`, which `shapeCandidate` assigns as index+1 — so it describes the order rather than testing it | **Right.** `promoteDecodings` and `extendsRecipe` are now exported and tested on synthetic candidates by **object identity**, which renumbering cannot satisfy. |
| CodeRabbit | The surface measurement inherits `CYBERCHEF_TOOL_ALLOWLIST` / `CYBERCHEF_EXPOSE_ALL_OPS`, which outrank `CYBERCHEF_TOOL_SURFACE` | **Right.** Both deleted from the child environment. |
| CodeRabbit | A SHA pin's comment is only checked for being non-empty, so `# current` passes and is then treated as a version | **Right.** Now validated against `^v\d+(\.\d+){0,2}$`. Verified by degrading a real `uses:` line — and the first attempt at that verification silently patched the explanatory prose instead, which is why it appeared to pass. |
| CodeRabbit | Bump the Helm chart's own `version` for this release | **Rejected on the code, accepted on the docs.** `bump-version.mjs` states it deliberately does not touch it ("it tracks the chart, not the app") and `check-version-consistency.mjs` reports it as "not checked, moves with the templates". No template changed this release. But **AGENTS.md said the opposite**, which is what the reviewer read — so the row was wrong and is corrected. A right question asked of a wrong row. |
| CodeRabbit | The Helm workload ignores `image.digest`, so nodes can pull mutated bytes | **Rejected, false on both halves.** `templates/workload.yaml:77-78` has `{{- if .Values.image.digest }} image: "repository@digest"`, and `helm-chart.yml` already renders **both** cases — including an explicit guard that a template always rendering a digest would pass the digest check and still be wrong. Added in v3.2.0. |

Five right, two wrong, one documentation defect surfaced by a wrong-looking question. The one that
matters most is the first: a fix for a reporting bug introduced a data bug three steps away in the
same file, and only a reviewer reading the whole workflow caught it.

---

## F-15 — `docker/setup-qemu-action@v4` hangs the multi-arch release build

The release itself, after the merge. The v3.9.0 tag's build hung twice at "Build and push Docker
image to both registries", 33 minutes each, with nothing pushed to any registry. Measured across
three runs of the same commit content:

```text
setup-qemu-action@v3   build  378s   success   (v3.8.0)
setup-qemu-action@v4   build 1979s   HUNG      (v3.9.0, twice)
setup-qemu-action@v3   build  399s   success   (v3.9.0, after the revert)
```

399s against a 378s baseline is 5.5% -- ordinary variance. Same Dockerfile, same lockfile, same
runner pool. The only difference between hanging indefinitely and finishing in six and a half
minutes is this action's major version, taken from Dependabot #127 during this release.

### The trap, which cost two wasted runs

**The v4 step itself succeeds, in 8 seconds** — against 4 seconds for v3, comparably fast, and
green. I read that as evidence the action was fine and eliminated it as a hypothesis. It is not:
installing binfmt handlers succeeds, and whether the *resulting emulation* works is not something
the step measures. A green setup step and a working emulator are different claims.

That is the same shape as this release's other three findings — a check reporting something other
than what a reader takes it to mean — arriving one more time, in the release of the fix.

### What was eliminated before the revert, and how

Three hypotheses, all tested rather than argued:

| Hypothesis | Test | Result |
|---|---|---|
| The new base digests or lockfile broke the build | Build `Dockerfile.mcp` locally for amd64 | **Clean.** All ten runtime stages, exit 0. |
| I pinned a single-arch base digest | Fetch both digests from `cgr.dev` with an index Accept header | **Both are `application/vnd.oci.image.index.v1+json` carrying linux/amd64 AND linux/arm64.** |
| The QEMU action broke | Compare step duration and conclusion | **8s, success** — which is what made it *look* eliminated. |

Two further readings were asserted and withdrawn during the investigation: that the workflow diff
was "decisive" (three inputs feed that step, not one), and that a build step producing no buildx
output proved a stall (that is what any cancelled step with collapsed log groups looks like).

### Fixed, and fenced

Pinned to v3 with the numbers in the file, and `.github/dependabot.yml` now ignores
`docker/setup-qemu-action >= 4` so it cannot return silently on a Sunday — the same mechanism
already holding `@xmldom/xmldom`, `geodesy` and `jq-web`, and for the same reason: a hold is only
honest if the evidence sits beside it.

Two improvements landed alongside, neither of them the fix:

- **`timeout-minutes: 45`** on the job. There was none, so a hang would have burned the 6-hour
  default. ~7x the median.
- **`BUILDKIT_PROGRESS: plain`** on the build step. The default mode rewrites output in place and
  flushes nothing on cancel, which is precisely why both hangs produced no diagnosable output.

### One more thing the tag showed

`npm view` reported 3.8.0 for several minutes *after* a successful publish, and the registry's
`modified` timestamp still read 2026-09-05. I took that as a silent publish failure. It was
propagation lag, and the step log said so plainly:

```text
+ cyberchef-mcp@3.9.0
npm notice Your package is being processed and may take a few minutes to become available.
```

Reading the log settled in seconds what the registry query had made look like a defect.
