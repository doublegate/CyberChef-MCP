apiVersion: v1
kind: Service
metadata:
  name: {{ include "cyberchef-mcp.fullname" . }}
  labels:
    {{- include "cyberchef-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cyberchef-mcp.selectorLabels" . | nindent 4 }}
